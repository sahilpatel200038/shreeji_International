# Shreeji International Frontend

React 19 + Vite + TypeScript + Tailwind marketing site with an integrated shipment tracking UI
and a small admin panel for managing shipments.

Run from the monorepo root with `npm run dev` (starts this together with the Laravel API), or
standalone from this directory with `npm run dev`.

See the root [README.md](../../README.md) for full setup instructions.
